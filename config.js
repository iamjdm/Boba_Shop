// Empty string = same-origin. Works automatically in both local dev (served
// from Flask at http://127.0.0.1:5000) and production (Render URL).
// NOTE: open HTML files directly in a browser without Flask and the API won't connect.
const API_BASE = "";
